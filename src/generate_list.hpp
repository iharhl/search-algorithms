#ifndef GENERATE_LIST_H
#define GENERATE_LIST_H

#include <algorithm>
#include <ctime>
#include <cstdlib> 
#include <vector>

void generate_list(std::vector<int>& randArray);
int random_number();

#endif